@Library("local-shared-library") _
pipeline {
    agent any
    stages {
	stage('Init')
	{
        steps {
               script 
               {
               
                 helloWorld.call()
               }
            }
    }
   }   
}
